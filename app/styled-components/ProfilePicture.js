/* @flow */

import styled from 'styled-components';

const ProfilePicture = styled.img`
  width: 3.5rem;
  height: 3.5rem;
  border-radius: 50%;
  &:hover {
    cursor: pointer;
  }
`;

export default ProfilePicture;
